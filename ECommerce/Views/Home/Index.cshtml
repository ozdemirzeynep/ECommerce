@model List<Category>

@{await Html.RenderPartialAsync("_Partial_Categories");}

    <p>
        <a href="@Url.Action("Help", "Home")">Yardım Masasına Git</a><br />
        <a href="@Url.Action("Contact", "Home")">İletişim İçin Tıklayın</a>
    </p>

